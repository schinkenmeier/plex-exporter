{
  "name": "plex-exporter",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend",
    "packages/*",
    "tools"
  ],
  "scripts": {
    "prepare": "node -e \"if (process.env.CI !== 'true') { const { execSync } = require('child_process'); try { execSync('husky install', { stdio: 'inherit' }); } catch (e) { /* ignore */ } }\"",
    "test": "npm test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "echo 'Run linting via GitHub Actions'",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
