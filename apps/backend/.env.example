# Server Configuration
NODE_ENV=development
PORT=4001

# Database
SQLITE_PATH=../../data/sqlite/plex-exporter.sqlite

# SMTP Configuration (optional - all fields must be set together)
# Uncomment and configure these values to enable email notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-username
# SMTP_PASS=your-password
# SMTP_FROM=plex-exporter@example.com
# SMTP_SECURE=false

# Tautulli Integration (optional - both values must be set together)
# Uncomment and configure these values to enable Tautulli integration
# TAUTULLI_URL=https://tautulli.example.com
# TAUTULLI_API_KEY=your-tautulli-api-key
